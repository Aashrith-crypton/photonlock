import MissionNav, { MobileMissionNav } from "@/components/MissionNav";
import { Aperture, ArrowDown, Camera, Cpu, RadioTower, Waves } from "lucide-react";

const pipeline = [["Beacon", RadioTower], ["Virtual camera", Camera], ["Threshold / centroid", Aperture], ["Kalman state estimation", Waves], ["Offline GRU prediction", Cpu], ["Pan / tilt alignment", Aperture]];

export default function TechnicalBrief() {
  return <div className="lab-shell lg:flex"><MissionNav active="technical" /><main className="mission-content"><MobileMissionNav active="technical" /><header className="page-header"><div><p className="eyebrow">TECHNICAL BRIEF</p><h1>Physics-aware virtual coarse alignment.</h1><p>PhotonLock is a deterministic virtual laboratory for demonstrating the acquisition and tracking of mobile optical beacons in a Free-Space Optical Communication scenario.</p></div></header><section className="technical-doc"><article><p className="eyebrow">WHAT IS FSOC / PAT?</p><h2>Optical links need a reliable pointing loop.</h2><p>Free-Space Optical Communication transfers information through a directed optical beam. Pointing, Acquisition, and Tracking (PAT) aligns a terminal with its counterpart while relative motion and environmental disturbance change the line of sight.</p><p>PhotonLock concentrates on the coarse-alignment loop: a virtual camera observes a designated optical beacon, threshold/blob/centroid perception provides a measurement, a Kalman estimator filters it, and a virtual pan/tilt controller reacts. Predictive mode may use the documented offline GRU artifact alongside the kinematic baseline.</p></article><article className="technical-panel"><p className="eyebrow">IMPLEMENTED SIGNAL PATH</p><div className="pipeline">{pipeline.map(([label, Icon], index) => { const StepIcon = Icon as typeof Aperture; return <div key={label as string}><StepIcon className="h-4 w-4 text-cyan-200" /><span>{label as string}</span>{index < pipeline.length - 1 ? <ArrowDown className="h-3.5 w-3.5 text-slate-600" /> : null}</div>; })}</div></article></section><section className="overview-grid"><article className="technical-panel"><p className="eyebrow">SUPPORTED DISTURBANCES</p><h3>Noise, vibration, turbulence-like distortion, latency, target speed, target count, and configured occlusion.</h3><p>Each listed condition maps to a current simulation configuration. Unsupported physical effects are not represented as measured parameters.</p></article><article className="technical-panel"><p className="eyebrow">EVALUATION</p><h3>Acquisition, tracking error, lock retention, loss/reacquisition, prediction residual, FPS, and measured latency.</h3><p>All displayed outcome values are calculated from executed simulator frames. Hardware feed mode is safety-gated and remains an adapter surface until a verified local bridge is configured.</p></article></section></main></div>;
}
